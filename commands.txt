git init
git clon
git status
git merge
git switch
git add "send the individual files from working directory to staging area " git add file_name
git commit -m "message"  "send the branch of files from staging area to local repository"
git remote add origin url "adding the remote repository to local repository"
git push -u origin master(branch) "push the files into remote repository"
git checkout "jump from one branch to another branch"
git checkout -b "used to creat a new branch"